
https://codepen.io/samuel-aberra/pen/RwamKXG?editors=1100

 1. What is Semantic HTML? Semantic elements describe what they are doing
    2. What is HTML used for? Html is a mark up language that is used for web development.
    3. What is an attribute and where do we put it? Provide additional information about the content of an element and it appears in the opening tags
    4. What is the h1 tag used for? How many times should I use it on a page?h1 is header one or title; you can use it as many times as you want
    5. Name two tags that have required attributes <img>, <a>
    6. What do we put in the head of our HTML document? <!DOCTYPE hmtl>
    7. What is an id? Its an attribute that is used when you are trying to recognize html element
    8. What elements can I add an id to? <h1>, <p>, <section>, <nav>, <header>, <footer>
    9. How many times can I use the same id on a page? 1
    10. What is a class? It selects elements with specific class attributes.
    11. What elements can I add a class to? <Span>, <div>, ,p>, <h>
    12. How many times can I use the same class on a page? As many as you want 
    13. How do I get my link to open in a new tab? target="_blank"
    14. What is the alt attribute in the image tag used for? If the image cannot be seen
    15. How do I reference an id? 
    16. What is the difference between a section and a div? section is for the different sections whereas div is for dividing sections.
    17. What is CSS used for? CSS is used for styling
    18. How to we select an element? Example - every h2 on the page
    19. What is the difference between a class and an id? - Give me an example of when I might use each one? ids are unique and classes are not unique.  
    20. How do we select classes in CSS? .classes
    21. How do we select a p element with a single class of “human””? .human p{}
    22. What is a parent child selector? When would this be useful? Selects all elements that are a direct child of the specified element. I think its useful with lists
    23. How do you select all links within a div with the class of sidebar?
    24. What is a pseudo selector?it defines a special state of an element
    25. What do we use the change the spacing between lines? margin
    26. What do we use to change the spacing between letters?letter-spacing:
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? text-transform: uppercase; text-transform: lowercase; text-transform: capitalize;
    28. How do I add a 1px border around my div that is dotted and black? border: dotted 1px black;
    29. How do I select everything on the page? Command a
    30. How do I write a comment in CSS?/* */
    31. How do I find out what file I am in, when I am using the command line? $ pwd
    32. Using the command line - how do I see a list of files/folders in my current folder? $ls
    33. How do I remove a file via the command line? Why do I have to be careful with this? $rm
    34. Why should I use version control? So I can go back to a previous version if I completely mess the current version up
    35. How often should I commit to github? 
    36. What is the command we would use to push our repo up to github? git push -u origin branch-name
    37. Walk me through Lambda's git flow. 
Fork
Clone
Create a branch with git checkout -b 'firstname- lastname'
Push 
Pull Request

Stretch Questions

    1. What is the difference between an inline element and a block element? block elements force a new line while inline element doesn't
    2. What happens when an element is positioned absolutely? The element is fixed
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width? Margin
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default (<h>,<p>,<div>), (<span>,<nav>)
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
